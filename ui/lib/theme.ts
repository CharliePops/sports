import { createMuiTheme } from "@material-ui/core/styles";

// Create a theme instance.
const theme = createMuiTheme({
  // TODO: override theme defaults
  // https://material-ui.com/customization/default-theme/
});

export default theme;
